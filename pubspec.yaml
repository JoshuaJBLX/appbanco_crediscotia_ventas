name: appbanco_crediscotia_ventas
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase Core (versión estable)
  firebase_core: ^2.27.0
  
  # Firebase Services
  cloud_firestore: ^4.17.0
  firebase_auth: ^4.17.0
  
  # NO incluyas firebase_functions por ahora
  
  # UI & State
  provider: ^6.1.2
  
  # Local Storage
  shared_preferences: ^2.2.2
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true