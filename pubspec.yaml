name: appbanco_crediscotia_ventas
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^2.27.0
  cloud_firestore: ^4.17.0
  firebase_auth: ^4.17.0
  
  # UI & State
  provider: ^6.1.2
  
  # Local Storage
  shared_preferences: ^2.2.2
  crypto: ^3.0.3
  
  # SPRINT 2 - Mapas y Rutas
  google_maps_flutter: ^2.14.2
  geolocator: ^12.0.0
  geocoding: ^4.0.0
  url_launcher: ^6.3.2
  permission_handler: ^12.0.3
  http: ^1.3.0
  connectivity_plus: ^7.1.1
  sqflite: ^2.4.2+1
  path: ^1.9.1
  path_provider: ^2.1.5
  flutter_map: ^6.1.0
  latlong2: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true